# SingleWaveReconstructionData

Waveform data repository for single-waveform time-series modeling via kernel-based dynamic mode decomposition (KDMD) for real-time dynamics reconstruction.

## Incoming datasets

Two additional waveform datasets are prepared for upload:

- `kicers/`
- `moudlator/`

Directory names intentionally match the incoming dataset labels.

Place the uploaded dataset contents into their matching directories after extracting the zip files.
